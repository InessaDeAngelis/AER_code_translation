* install packages
	
	cap which cibar
	if _rc ssc install cibar
	
	cap which reghdfe
	if _rc ssc install reghdfe
	
	cap which dataout
	if _rc ssc install dataout
	
	cap which binscatter
	if _rc ssc install binscatter

	cap which coefplot
	if _rc ssc install coefplot
	
	
	cap which estout
	if _rc ssc install estout
	
	cap which outreg
	if _rc ssc install outreg
	
	cap which outreg2
	if _rc ssc install outreg2

	cap which  tabout
	if _rc ssc install tabout


	cap which  fsum
	if _rc ssc install fsum
	
	cap which  ritest
	if _rc ssc install ritest
	
	cap which  ftools
	if _rc ssc install ftools
		
	cap which  gtools
	if _rc ssc install gtools
